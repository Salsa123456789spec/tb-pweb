
<div class="container mx-auto mt-8 px-4 sm:px-6 lg:px-8">
    <h2 class="text-3xl font-bold text-gray-800 mb-6">Penugasan</h2>

    <% if (success_msg && success_msg.length > 0) { %>
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-4" role="alert">
            <%= success_msg %>
        </div>
    <% } %>
    <% if (error_msg && error_msg.length > 0) { %>
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4" role="alert">
            <%= error_msg %>
        </div>
    <% } %>

    <p class="text-gray-600 mb-6">
        Anda bisa melihat pembagian kelompok magang pada laman hasil seleksi wawancara
        atau bisa dengan mengklik <a href="#" class="text-blue-600 hover:underline">disini</a>
    </p>

    <% if (assignments.length > 0) { %>
        <div class="flex flex-wrap -mx-2">
            <% assignments.forEach(assignment => { %>
                <div class="w-full md:w-1/2 lg:w-1/2 px-2 mb-4">
                    <div class="bg-white rounded-lg shadow-md overflow-hidden">
                        <div class="p-6">
                            <h5 class="flex justify-between items-center text-lg font-semibold text-gray-800 mb-2">
                                <%= assignment.judul %>
                                <% if (!assignment.submitted) { %>
                                    <span class="bg-red-500 text-white text-xs font-semibold px-2 py-1 rounded-full">Belum dikumpul</span>
                                <% } else { %>
                                    <span class="bg-green-500 text-white text-xs font-semibold px-2 py-1 rounded-full">Sudah dikumpul</span>
                                <% } %>
                            </h5>
                            <p class="text-gray-600 text-sm mb-4">Tenggat: <%= assignment.deadline.toLocaleDateString('id-ID', { year: 'numeric', month: 'long', day: 'numeric' }) %></p>
                            <a href="/mahasiswa/<%= assignment.id %>" class="inline-block bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded text-sm transition duration-150 ease-in-out">Tampilkan detail</a>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
    <% } else { %>
        <p class="text-gray-600">Belum ada penugasan saat ini.</p>
    <% } %>
</div>