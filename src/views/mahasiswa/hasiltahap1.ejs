<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hasil Tahap 1 - Ujian Tertulis</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-gradient-to-br from-blue-50 via-white to-indigo-50 min-h-screen">
    <div class="container mx-auto px-4 py-8 max-w-7xl">
        <!-- Header -->
        <div class="mb-8 text-center">
            <div class="inline-flex items-center justify-center w-16 h-16 bg-blue-500 rounded-full mb-4">
                <i class="fas fa-clipboard-list text-white text-2xl"></i>
            </div>
            <h1 class="text-4xl font-bold text-gray-800 mb-2">Hasil Tahap 1</h1>
            <p class="text-xl text-gray-600">Verifikasi Berkas - Program Magang Enterprise Application Development</p>
            <div class="w-24 h-1 bg-blue-500 rounded-full mx-auto mt-4"></div>
        </div>

        <!-- Main Content -->
        <div class="bg-white rounded-2xl shadow-xl border border-gray-100 overflow-hidden">
            <div class="bg-gradient-to-r from-blue-500 to-indigo-600 px-8 py-6">
                <div class="flex items-center text-white">
                    <div class="flex-shrink-0">
                        <i class="fas fa-clipboard-list text-3xl"></i>
                    </div>
                    <div class="ml-4">
                        <h2 class="text-2xl font-bold">Hasil Tahap 1 - Verifikasi Berkas</h2>
                        <p class="text-blue-100 mt-1">Daftar peserta yang lolos verifikasi berkas</p>
                    </div>
                </div>
            </div>

            <div class="p-8">
                <!-- Status Message -->
                <% 
                let userPassedTahap1 = false;
                if (pengumumanTahap1 && pengumumanTahap1.length > 0) {
                    userPassedTahap1 = pengumumanTahap1.some(p => p.user_id === user.id);
                }
                %>

                <% if (userPassedTahap1) { %>
                <div class="mb-8 bg-gradient-to-r from-green-50 to-emerald-50 border-l-4 border-green-500 rounded-lg p-6 shadow-sm">
                    <div class="flex items-center">
                        <div class="flex-shrink-0">
                            <i class="fas fa-check-circle text-green-500 text-2xl"></i>
                        </div>
                        <div class="ml-4">
                            <h3 class="text-xl font-bold text-green-800">Selamat! Anda Lolos Tahap 1</h3>
                            <p class="text-green-700 mt-2 text-lg">Anda berhasil melewati verifikasi berkas dan memenuhi persyaratan untuk melanjutkan ke tahap wawancara.</p>
                        </div>
                    </div>
                </div>
                <% } else if (pengumumanTahap1 && pengumumanTahap1.length > 0) { %>
                <div class="mb-8 bg-gradient-to-r from-red-50 to-rose-50 border-l-4 border-red-500 rounded-lg p-6 shadow-sm">
                    <div class="flex items-center">
                        <div class="flex-shrink-0">
                            <i class="fas fa-times-circle text-red-500 text-2xl"></i>
                        </div>
                        <div class="ml-4">
                            <h3 class="text-xl font-bold text-red-800">Maaf, Anda Tidak Lolos Tahap 1</h3>
                            <p class="text-red-700 mt-2 text-lg">Verifikasi berkas Anda belum memenuhi standar yang ditetapkan. Silakan coba lagi pada periode selanjutnya.</p>
                        </div>
                    </div>
                </div>
                <% } else { %>
                <div class="mb-8 bg-gradient-to-r from-yellow-50 to-amber-50 border-l-4 border-yellow-500 rounded-lg p-6 shadow-sm">
                    <div class="flex items-center">
                        <div class="flex-shrink-0">
                            <i class="fas fa-clock text-yellow-500 text-2xl"></i>
                        </div>
                        <div class="ml-4">
                            <h3 class="text-xl font-bold text-yellow-800">Pengumuman Belum Tersedia</h3>
                        </div>
                    </div>
                </div>
                <% } %>

                <!-- Table -->
                <% if (pengumumanTahap1 && pengumumanTahap1.length > 0) { %>
                <div class="bg-gray-50 rounded-xl p-6">
                    <div class="flex items-center justify-between mb-6">
                        <h3 class="text-xl font-bold text-gray-800">Daftar Peserta Yang Lolos</h3>
                        <div class="flex items-center space-x-4">
                            <div class="flex items-center space-x-2 text-sm text-gray-500">
                                <i class="fas fa-info-circle"></i>
                                <span>Total: <%= pengumumanTahap1.length %> peserta</span>
                            </div>
                            <a href="/mahasiswa/generate-pdf-tahap-1" 
                               class="bg-red-500 hover:bg-red-600 text-white font-bold py-2 px-4 rounded-lg transition-all duration-200 hover:scale-105 flex items-center space-x-2">
                                <i class="fas fa-file-pdf"></i>
                                <span>Download PDF</span>
                            </a>
                        </div>
                    </div>
                    
                    <div class="overflow-x-auto">
                        <table class="w-full">
                            <thead>
                                <tr class="bg-gray-800 text-white">
                                    <th class="px-8 py-4 text-left text-sm font-bold uppercase tracking-wider rounded-tl-lg">No</th>
                                    <th class="px-8 py-4 text-left text-sm font-bold uppercase tracking-wider">Nama Lengkap</th>
                                    <th class="px-8 py-4 text-left text-sm font-bold uppercase tracking-wider">Divisi</th>
                                    <th class="px-8 py-4 text-left text-sm font-bold uppercase tracking-wider">NIM Mahasiswa</th>
                                    <th class="px-8 py-4 text-left text-sm font-bold uppercase tracking-wider rounded-tr-lg">Status</th>
                                </tr>
                            </thead>
                            <tbody class="bg-white">
                                <% pengumumanTahap1.forEach((item, index) => { %>
                                <tr class="<%= item.user_id === user.id ? 'bg-blue-50 border-l-4 border-blue-500' : 'hover:bg-gray-50' %> transition-colors border-b border-gray-200">
                                    <td class="px-8 py-6 text-lg font-semibold text-gray-900"><%= index + 1 %></td>
                                    <td class="px-8 py-6">
                                        <div class="flex items-center">
                                            <div class="flex-shrink-0 w-12 h-12 bg-blue-500 rounded-full flex items-center justify-center">
                                                <span class="text-white font-bold text-lg"><%= item.user.nama_lengkap.charAt(0).toUpperCase() %></span>
                                            </div>
                                            <div class="ml-4">
                                                <div class="text-lg font-bold text-gray-900"><%= item.user.nama_lengkap %></div>
                                                <div class="text-sm text-gray-500">ID: <%= item.user.id %></div>
                                            </div>
                                        </div>
                                    </td>
                                    <td class="px-8 py-6">
                                        <span class="inline-flex px-3 py-1 text-sm font-semibold rounded-full bg-indigo-100 text-indigo-800">
                                            <%= item.pendaftaran.divisi %>
                                        </span>
                                    </td>
                                    <td class="px-8 py-6 text-lg text-gray-900">
                                        <% if (item.user && item.user.nim) { %>
                                            <%= item.user.nim %>
                                        <% } else { %>
                                            <span class="text-gray-400 italic">Tidak tersedia</span>
                                        <% } %>
                                    </td>
                                    <td class="px-8 py-6">
                                        <span class="inline-flex items-center px-4 py-2 text-sm font-bold rounded-full bg-green-100 text-green-800">
                                            <i class="fas fa-check mr-2"></i>LOLOS
                                        </span>
                                    </td>
                                </tr>
                                <% }); %>
                            </tbody>
                        </table>
                    </div>
                </div>
                <% } %>

                <!-- Footer Information -->
                <div class="mt-8 bg-blue-50 rounded-xl p-6">
                    <div class="flex items-start">
                        <i class="fas fa-info-circle text-blue-500 text-xl mr-3 mt-1"></i>
                        <div>
                            <h4 class="text-lg font-semibold text-blue-800 mb-2">Informasi Penting</h4>
                            <ul class="text-blue-700 space-y-1">
                                <li>• Peserta yang lolos akan melanjutkan ke tahap wawancara</li>
                                <li>• Jadwal wawancara akan dikirimkan melalui email dalam 2-3 hari kerja</li>
                                <li>• Pastikan email dan nomor telepon Anda aktif</li>
                                <li>• Untuk pertanyaan lebih lanjut, hubungi panitia penyelenggara</li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>