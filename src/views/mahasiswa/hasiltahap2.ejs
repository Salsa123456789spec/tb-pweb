<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hasil Tahap 2 - Wawancara</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-gradient-to-br from-green-50 via-white to-emerald-50 min-h-screen">
    <div class="container mx-auto px-4 py-8 max-w-7xl">
        <!-- Header -->
        <div class="mb-8 text-center">
            <div class="inline-flex items-center justify-center w-16 h-16 bg-green-500 rounded-full mb-4">
                <i class="fas fa-user-check text-white text-2xl"></i>
            </div>
            <h1 class="text-4xl font-bold text-gray-800 mb-2">Hasil Tahap 2</h1>
            <p class="text-xl text-gray-600">Wawancara - Program Magang Enterprise Application Development</p>
            <div class="w-24 h-1 bg-green-500 rounded-full mx-auto mt-4"></div>
        </div>

        <!-- Main Content -->
        <div class="bg-white rounded-2xl shadow-xl border border-gray-100 overflow-hidden">
            <div class="bg-gradient-to-r from-green-500 to-emerald-600 px-8 py-6">
                <div class="flex items-center text-white">
                    <div class="flex-shrink-0">
                        <i class="fas fa-user-check text-3xl"></i>
                    </div>
                    <div class="ml-4">
                        <h2 class="text-2xl font-bold">Hasil Tahap 2 - Wawancara</h2>
                        <p class="text-green-100 mt-1">Daftar peserta yang lolos tahap wawancara</p>
                    </div>
                </div>
            </div>

            <div class="p-8">
                <!-- Status Message -->
                <% 
                let userPassedTahap1 = false;
                let userPassedTahap2 = false;
                
                if (pengumumanTahap1 && pengumumanTahap1.length > 0) {
                    userPassedTahap1 = pengumumanTahap1.some(p => p.user_id === user.id);
                }
                
                if (pengumumanTahap2 && pengumumanTahap2.length > 0) {
                    userPassedTahap2 = pengumumanTahap2.some(p => p.user_id === user.id);
                }
                %>

                <% if (userPassedTahap1) { %>
                    <% if (userPassedTahap2) { %>
                    <div class="mb-8 bg-gradient-to-r from-green-50 to-emerald-50 border-l-4 border-green-500 rounded-lg p-6 shadow-sm">
                        <div class="flex items-center">
                            <div class="flex-shrink-0">
                                <i class="fas fa-check-circle text-green-500 text-2xl"></i>
                            </div>
                            <div class="ml-4">
                                <h3 class="text-xl font-bold text-green-800">Selamat! Anda Lolos Tahap 2</h3>
                                <p class="text-green-700 mt-2 text-lg">Anda berhasil melewati tahap wawancara dengan baik. Bersiaplah untuk tahap penerimaan magang.</p>
                            </div>
                        </div>
                    </div>
                    <% } else if (pengumumanTahap2 && pengumumanTahap2.length > 0) { %>
                    <div class="mb-8 bg-gradient-to-r from-red-50 to-rose-50 border-l-4 border-red-500 rounded-lg p-6 shadow-sm">
                        <div class="flex items-center">
                            <div class="flex-shrink-0">
                                <i class="fas fa-times-circle text-red-500 text-2xl"></i>
                            </div>
                            <div class="ml-4">
                                <h3 class="text-xl font-bold text-red-800">Maaf, Anda Tidak Lolos Tahap 2</h3>
                                <p class="text-red-700 mt-2 text-lg">Terima kasih atas partisipasi Anda dalam tahap wawancara. Tetap semangat untuk kesempatan berikutnya.</p>
                            </div>
                        </div>
                    </div>
                    <% } else { %>
                    <div class="mb-8 bg-gradient-to-r from-yellow-50 to-amber-50 border-l-4 border-yellow-500 rounded-lg p-6 shadow-sm">
                        <div class="flex items-center">
                            <div class="flex-shrink-0">
                                <i class="fas fa-clock text-yellow-500 text-2xl"></i>
                            </div>
                            <div class="ml-4">
                                <h3 class="text-xl font-bold text-yellow-800">Pengumuman Belum Tersedia</h3>
                            </div>
                        </div>
                    </div>
                    <% } %>
                <% } %>

                <!-- Table -->
                <% if (pengumumanTahap2 && pengumumanTahap2.length > 0) { %>
                <div class="bg-gray-50 rounded-xl p-6">
                    <div class="flex items-center justify-between mb-6">
                        <h3 class="text-xl font-bold text-gray-800">Daftar Peserta Yang Lolos</h3>
                        <div class="flex items-center space-x-4">
                            <div class="flex items-center space-x-2 text-sm text-gray-500">
                                <i class="fas fa-info-circle"></i>
                                <span>Total: <%= pengumumanTahap2.length %> peserta</span>
                            </div>
                            <a href="/mahasiswa/generate-pdf-tahap-2" 
                               class="bg-red-500 hover:bg-red-600 text-white font-bold py-2 px-4 rounded-lg transition-all duration-200 hover:scale-105 flex items-center space-x-2">
                                <i class="fas fa-file-pdf"></i>
                                <span>Download PDF</span>
                            </a>
                        </div>
                    </div>
                    
                    <div class="overflow-x-auto">
                        <table class="w-full">
                            <thead>
                                <tr class="bg-gray-800 text-white">
                                    <th class="px-8 py-4 text-left text-sm font-bold uppercase tracking-wider rounded-tl-lg">No</th>
                                    <th class="px-8 py-4 text-left text-sm font-bold uppercase tracking-wider">Nama Lengkap</th>
                                    <th class="px-8 py-4 text-left text-sm font-bold uppercase tracking-wider">Divisi</th>
                                    <th class="px-8 py-4 text-left text-sm font-bold uppercase tracking-wider">NIM Mahasiswa</th>
                                    <th class="px-8 py-4 text-left text-sm font-bold uppercase tracking-wider rounded-tr-lg">Status</th>
                                </tr>
                            </thead>
                            <tbody class="bg-white">
                                <% pengumumanTahap2.forEach((item, index) => { %>
                                <tr class="<%= item.user_id === user.id ? 'bg-green-50 border-l-4 border-green-500' : 'hover:bg-gray-50' %> transition-colors border-b border-gray-200">
                                    <td class="px-8 py-6 text-lg font-semibold text-gray-900"><%= index + 1 %></td>
                                    <td class="px-8 py-6">
                                        <div class="flex items-center">
                                            <div class="flex-shrink-0 w-12 h-12 bg-green-500 rounded-full flex items-center justify-center">
                                                <span class="text-white font-bold text-lg"><%= item.user.name.charAt(0).toUpperCase() %></span>
                                            </div>
                                            <div class="ml-4">
                                                <div class="text-lg font-bold text-gray-900"><%= item.user.name %></div>
                                                <div class="text-sm text-gray-500">ID: <%= item.user.id %></div>
                                            </div>
                                        </div>
                                    </td>
                                    <td class="px-8 py-6">
                                        <span class="inline-flex px-3 py-1 text-sm font-semibold rounded-full bg-indigo-100 text-indigo-800">
                                            <%= item.pendaftaran.divisi %>
                                        </span>
                                    </td>
                                    <td class="px-8 py-6 text-lg text-gray-900">
                                        <% if (item.user && item.user.nim) { %>
                                            <%= item.user.nim %>
                                        <% } else { %>
                                            <span class="text-gray-400 italic">Tidak tersedia</span>
                                        <% } %>
                                    </td>
                                    <td class="px-8 py-6">
                                        <span class="inline-flex items-center px-4 py-2 text-sm font-bold rounded-full bg-green-100 text-green-800">
                                            <i class="fas fa-check mr-2"></i>LOLOS
                                        </span>
                                    </td>
                                </tr>
                                <% }); %>
                            </tbody>
                        </table>
                    </div>
                </div>
                <% } %>

                <!-- Footer Information -->
                <div class="mt-8 bg-green-50 rounded-xl p-6">
                    <div class="flex items-start">
                        <i class="fas fa-info-circle text-green-500 text-xl mr-3 mt-1"></i>
                        <div>
                            <h4 class="text-lg font-semibold text-green-800 mb-2">Informasi Penting</h4>
                            <ul class="text-green-700 space-y-1">
                                <li>• Peserta yang lolos akan melanjutkan ke tahap akhir penerimaan magang</li>
                                <li>• Pengumuman hasil akhir akan diumumkan dalam 1 minggu</li>
                                <li>• Informasi penempatan dan jadwal magang akan diberikan setelah diterima</li>
                                <li>• Pastikan kontak Anda selalu aktif untuk komunikasi lebih lanjut</li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>