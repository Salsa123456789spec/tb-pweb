<div class="container mx-auto p-4">
    <h1 class="text-2xl font-bold mb-4">Penugasan</h1>
    <p class="mb-6">Anda bisa melihat pembagian kelompok magang pada laman hasil seleksi wawancara atau bisa dengan mengklik <a href="#" class="text-blue-600">disini</a>.</p>

    <% if (tugas.length > 0) { %>
        <% tugas.forEach(item => { %>
            <div class="bg-white border border-gray-200 rounded-lg p-4 mb-4">
                <div class="flex justify-between items-center mb-2">
                    <h2 class="text-lg font-semibold"><%= item.judul %></h2>
                    <span class="bg-red-500 text-white text-xs font-semibold mr-2 px-2.5 py-0.5 rounded">Belum dikumpul</span>
                </div>
                <p class="text-sm text-gray-600 mb-4">Tenggat <%= new Date(item.deadline).toLocaleDateString('id-ID', { day: 'numeric', month: 'long', year: 'numeric' }) %></p>
                <button class="border border-gray-300 text-gray-700 px-4 py-2 rounded-lg hover:bg-gray-100">Tampilkan detail</button>
            </div>
        <% }) %>
    <% } else { %>
        <p>Belum ada tugas yang tersedia.</p>
    <% } %>
</div>