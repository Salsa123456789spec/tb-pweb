

            <!-- Dashboard Cards -->
            <div class="grid grid-cols-1 lg:grid-cols-3 gap-4 mt-8">
                <!-- Card 1: Data Mahasiswa -->
                <div class="bg-white rounded-2xl shadow-lg p-6 border-t-4 border-purple-600 hover:shadow-xl transition-all duration-300">
                    <div class="flex items-center justify-between mb-4">
                        <div class="w-12 h-12 bg-gradient-to-br from-purple-600 to-purple-700 rounded-xl flex items-center justify-center">
                            <span class="text-white text-2xl">👥</span>
                        </div>
                        <div class="text-right">
                            <span class="text-sm text-gray-500">Total</span>
<div class="text-2xl font-bold text-purple-600"><%= totalUser %></div>

                        </div>
                    </div>
                    <h3 class="text-xl font-semibold text-gray-800 mb-3">Data Mahasiswa</h3>
                    <div class="space-y-2">
                        <div class="flex justify-between items-center">
                            <span class="text-gray-600">Aktif</span>
                           <span class="font-semibold text-green-600">5</span>

                        </div>
                        <div class="flex justify-between items-center">
                           <span class="font-semibold text-red-600">Tidak aktif</span>

                            <span class="font-semibold text-red-600">35</span>
                        </div>
                        <div class="mt-3 w-full bg-gray-200 rounded-full h-2">
                            <div class="bg-gradient-to-r from-green-500 to-green-600 h-2 rounded-full" style="width: 86%"></div>
                        </div>
                        <p class="text-xs text-gray-500 text-center">86% Mahasiswa Aktif</p>
                    </div>
                </div>

                <!-- Card 2: Data Aslab -->
                <div class="bg-white rounded-2xl shadow-lg p-6 border-t-4 border-blue-500 hover:shadow-xl transition-all duration-300">
                    <div class="flex items-center justify-between mb-4">
                        <div class="w-12 h-12 bg-gradient-to-br from-blue-500 to-blue-600 rounded-xl flex items-center justify-center">
                            <span class="text-white text-2xl">🧑‍🔬</span>
                        </div>
                        <div class="text-right">
                            <span class="text-sm text-gray-500">Total</span>
                            <div class="text-2xl font-bold text-blue-600">18</div>
                        </div>
                    </div>
                    <h3 class="text-xl font-semibold text-gray-800 mb-3">Data Aslab</h3>
                    <div class="space-y-2">
                        <div class="flex justify-between items-center">
                            <span class="text-gray-600">Web Development</span>
                            <span class="font-semibold text-blue-600">5</span>
                        </div>
                        <div class="flex justify-between items-center">
                            <span class="text-gray-600">Mobile Dev</span>
                            <span class="font-semibold text-blue-600">4</span>
                        </div>
                        <div class="flex justify-between items-center">
                            <span class="text-gray-600">Data Science</span>
                            <span class="font-semibold text-blue-600">3</span>
                        </div>
                        <div class="flex justify-between items-center">
                            <span class="text-gray-600">UI/UX Design</span>
                            <span class="font-semibold text-blue-600">6</span>
                        </div>
                    </div>
                </div>

                <!-- Card 3: Status Sistem -->
                <div class="bg-white rounded-2xl shadow-lg p-6 border-t-4 border-emerald-500 hover:shadow-xl transition-all duration-300">
                    <div class="flex items-center justify-between mb-4">
                        <div class="w-12 h-12 bg-gradient-to-br from-emerald-500 to-emerald-600 rounded-xl flex items-center justify-center">
                            <span class="text-white text-2xl">📊</span>
                        </div>
                        <div class="text-right">
                            <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-green-100 text-green-800">
                                AKTIF
                            </span>
                        </div>
                    </div>
                    <h3 class="text-xl font-semibold text-gray-800 mb-3">Status Sistem</h3>
                    <div class="space-y-3">
                        <div class="flex justify-between items-center">
                            <span class="text-gray-600">Pendaftaran</span>
                            <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-green-100 text-green-800">
                                Terbuka
                            </span>
                        </div>
                        <div class="flex justify-between items-center">
                            <span class="text-gray-600">Server Status</span>
                            <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-green-100 text-green-800">
                                Online
                            </span>
                        </div>
                        <div class="flex justify-between items-center">
                            <span class="text-gray-600">Database</span>
                            <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-green-100 text-green-800">
                                Connected
                            </span>
                        </div>
                        <div class="mt-4 p-3 bg-gray-50 rounded-lg">
                            <p class="text-sm text-gray-600">
                                <span class="font-medium">Uptime:</span> 99.8%
                            </p>
                            <p class="text-sm text-gray-600 mt-1">
                                <span class="font-medium">Last Update:</span> 2 menit lalu
                            </p>
                        </div>
                    </div>
                </div>
            </div>

           
    <script>
        // Add click handlers for sidebar menu
        document.querySelectorAll('.cursor-pointer').forEach(item => {
            item.addEventListener('click', function(e) {
                e.preventDefault();
                
                // Remove active class from all items
                document.querySelectorAll('nav a').forEach(link => {
                    link.classList.remove('bg-white', 'bg-opacity-20');
                    link.classList.add('bg-white', 'bg-opacity-10');
                });
                
                // Add active class to clicked item
                this.classList.remove('bg-white', 'bg-opacity-10');
                this.classList.add('bg-white', 'bg-opacity-20');
                
                // Get the menu text
                const menuText = this.querySelector('span:last-child').textContent;
                alert(`Navigasi ke halaman: ${menuText}`);
            });
        });

        // Simulate real-time updates
        function updateStats() {
            const stats = document.querySelectorAll('.text-2xl.font-bold');
            stats.forEach(stat => {
                if (Math.random() > 0.95) {
                    const currentValue = parseInt(stat.textContent);
                    if (!isNaN(currentValue)) {
                        const change = Math.random() > 0.5 ? 1 : -1;
                        stat.textContent = Math.max(0, currentValue + change);
                    }
                }
            });
        }

        // Update every 30 seconds
        setInterval(updateStats, 30000);
    </script>
</body>
</html>