<div class="p-6">
  <!-- Header -->
  <div class="flex justify-between items-center mb-6">
    <h1 class="text-3xl font-bold text-gray-800">Edit Data Aslab</h1>
    <a href="/superadmin/kelolaAslab" 
       class="bg-gray-600 hover:bg-gray-700 text-white px-4 py-2 rounded-lg flex items-center gap-2 transition">
      <span class="material-icons">arrow_back</span>
      Kembali
    </a>
  </div>

  <!-- Flash Messages -->
  <% if (error && error.length > 0) { %>
    <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4" role="alert">
      <span class="block sm:inline"><%= error %></span>
    </div>
  <% } %>

  <!-- Form -->
  <div class="bg-white rounded-lg shadow-md p-6">
    <form action="/superadmin/editAslab/<%= aslab.id %>" method="POST" class="space-y-6">
      <!-- Nama -->
      <div>
        <label for="name" class="block text-sm font-medium text-gray-700 mb-2">
          Nama Lengkap <span class="text-red-500">*</span>
        </label>
        <input type="text" 
               id="name" 
               name="name" 
               value="<%= aslab.name %>"
               required
               class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-green-500 focus:border-transparent"
               placeholder="Masukkan nama lengkap">
      </div>

      <!-- Email -->
      <div>
        <label for="email" class="block text-sm font-medium text-gray-700 mb-2">
          Email <span class="text-red-500">*</span>
        </label>
        <input type="email" 
               id="email" 
               name="email" 
               value="<%= aslab.email %>"
               required
               class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-green-500 focus:border-transparent"
               placeholder="Masukkan email">
      </div>

      <!-- No Aslab -->
      <div>
        <label for="no_aslab" class="block text-sm font-medium text-gray-700 mb-2">
          Nomor Aslab <span class="text-red-500">*</span>
        </label>
        <input type="text" 
               id="no_aslab" 
               name="no_aslab" 
               value="<%= aslab.no_aslab || '' %>"
               required
               class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-green-500 focus:border-transparent"
               placeholder="Masukkan nomor aslab">
      </div>

      <!-- Password Info -->
      <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4">
        <div class="flex items-start">
          <span class="material-icons text-yellow-600 mr-2 mt-0.5">info</span>
          <div>
            <h3 class="text-sm font-medium text-yellow-800">Password</h3>
            <p class="text-sm text-yellow-700 mt-1">
              Password tidak dapat diubah melalui form ini. Aslab dapat mengubah password mereka sendiri melalui menu profil.
            </p>
          </div>
        </div>
      </div>

      <!-- Submit Button -->
      <div class="flex justify-end space-x-4 pt-6 border-t border-gray-200">
        <a href="/superadmin/kelolaAslab" 
           class="px-6 py-2 border border-gray-300 rounded-md text-gray-700 hover:bg-gray-50 transition">
          Batal
        </a>
        <button type="submit" 
                class="px-6 py-2 bg-green-600 text-white rounded-md hover:bg-green-700 transition flex items-center gap-2">
          <span class="material-icons">save</span>
          Update
        </button>
      </div>
    </form>
  </div>

  <!-- Information -->
  <div class="mt-6 bg-blue-50 border border-blue-200 rounded-lg p-4">
    <div class="flex items-start">
      <span class="material-icons text-blue-600 mr-2 mt-0.5">info</span>
      <div>
        <h3 class="text-sm font-medium text-blue-800">Informasi</h3>
        <ul class="text-sm text-blue-700 mt-1 space-y-1">
          <li>• Semua field wajib diisi</li>
          <li>• Email harus unik dan belum terdaftar</li>
          <li>• Nomor aslab harus unik</li>
          <li>• Password tidak dapat diubah melalui form ini</li>
        </ul>
      </div>
    </div>
  </div>
</div> 